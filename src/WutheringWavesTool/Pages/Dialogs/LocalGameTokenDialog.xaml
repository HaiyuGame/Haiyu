<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="Haiyu.Pages.Dialogs.LocalGameTokenDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:Haiyu.Controls"
    xmlns:wrapper="using:Haiyu.Models.Wrapper"
    CornerRadius="5"
    mc:Ignorable="d">
    <Grid Width="450" Height="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock
            VerticalAlignment="Center"
            FontFamily="{ThemeResource DefaultFont}"
            FontSize="19"
            Text="本地游戏账号切换" />
        <Button
            HorizontalAlignment="Right"
            Command="{x:Bind ViewModel.CloseCommand}"
            CornerRadius="40"
            Style="{ThemeResource TransparentButtonStyle}">
            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE894;" />
        </Button>
        <ItemsView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Tokens, Mode=OneWay}">
            <ItemsView.ItemTemplate>
                <DataTemplate x:DataType="wrapper:KuroGameTokenWrapper">
                    <ItemContainer IsSelected="{x:Bind IsSelect, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <Grid
                            Padding="15"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource ControlSolidFillColorDefault}"
                            BorderThickness="1">
                            <TextBlock VerticalAlignment="Center" Text="{x:Bind Cache.Phone, Mode=OneWay}" />
                            <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Run Text="登录类型：" />
                                <Run Text="{x:Bind Cache.LoginType, Mode=OneWay}" />
                            </TextBlock>
                        </Grid>

                    </ItemContainer>
                </DataTemplate>
            </ItemsView.ItemTemplate>
            <ItemsView.Layout>
                <StackLayout Orientation="Vertical" Spacing="5" />
            </ItemsView.Layout>
        </ItemsView>
        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Center"
            Orientation="Horizontal"
            Spacing="5">
            <Button
                Padding="10,8"
                Command="{x:Bind ViewModel.SaveAndCloseCommand}"
                Content="保存"
                CornerRadius="10"
                FontSize="14"
                Style="{ThemeResource AccentButtonStyle}" />

        </StackPanel>
    </Grid>
</ContentDialog>

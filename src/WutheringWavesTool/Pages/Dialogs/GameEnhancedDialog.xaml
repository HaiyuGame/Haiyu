<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="Haiyu.Pages.Dialogs.GameEnhancedDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helper="using:Haiyu.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Haiyu.Pages.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    CornerRadius="5"
    mc:Ignorable="d">

    <Grid Width="400" Height="250">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Center" Orientation="Vertical">
            <FontIcon
                FontFamily="Segoe Fluent Icons"
                Foreground="{ThemeResource SystemColorGrayTextBrush}"
                Glyph="&#xEA6B;" />
            <TextBlock
                Margin="0,10,0,0"
                Foreground="{ThemeResource SystemColorGrayTextBrush}"
                Text="{x:Bind helper:LanguageService.GetString('Display_AdministratorPrivileges')}" />
        </StackPanel>
        <StackPanel
            Grid.Row="1"
            Margin="0,10,0,0"
            HorizontalAlignment="Stretch"
            Orientation="Vertical">
            <Grid
                Padding="10"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" Text="{x:Bind helper:LanguageService.GetString('Display_EnableXboxEnhancements')}" />
                <Border Grid.Column="1" HorizontalAlignment="Right">
                    <ToggleButton
                        x:Name="xboxEnable"
                        Checked="xboxEnable_Checked"
                        Content="{x:Bind helper:LanguageService.GetString('Display_ActivateTheLCC')}"
                        Tag="Xbox"
                        Unchecked="xboxEnable_Unchecked" />
                </Border>
            </Grid>
        </StackPanel>
        <Button
            Grid.Row="2"
            HorizontalAlignment="Stretch"
            Command="{x:Bind ViewModel.CloseCommand}"
            Content="{x:Bind helper:LanguageService.GetString('Display_Off')}" />
    </Grid>
</ContentDialog>

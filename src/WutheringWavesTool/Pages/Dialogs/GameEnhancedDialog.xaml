<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Haiyu.Pages.Dialogs.GameEnhancedDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Haiyu.Pages.Dialogs"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Height="250" Width="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition />
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Button Style="{ThemeResource TransparentButtonStyle}" HorizontalAlignment="Right" Command="{x:Bind ViewModel.CloseCommand}">
            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE894;" />
        </Button>
        <StackPanel Grid.Row="1" HorizontalAlignment="Stretch"  Orientation="Vertical">
            <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center"  Text="启用Xbox增强"/>
                <Border HorizontalAlignment="Right" Width="60" MaxWidth="60" Grid.Column="1">
                    <CheckBox  x:Name="xboxEnable" Tag="Xbox" >
                        <i:Interaction.Behaviors>
                            <i:EventTriggerBehavior EventName="Checked">
                                <i:InvokeCommandAction Command="{x:Bind ViewModel.EnableConfigCommand}"/>
                            </i:EventTriggerBehavior>
                            <i:EventTriggerBehavior EventName="Unchecked">
                                <i:InvokeCommandAction Command="{x:Bind ViewModel.EnableConfigCommand}"/>
                            </i:EventTriggerBehavior>
                        </i:Interaction.Behaviors>
                    </CheckBox>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</ContentDialog>

<?xml version="1.0" encoding="utf-8" ?>
<base:WindowModelBase
    x:Class="Haiyu.WindowModels.GetGeetWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helper="using:Haiyu.Helpers"
    xmlns:base="using:Haiyu.Common.Bases"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Haiyu.WindowModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:Haiyu.Controls"
    Title="GetgeetWindow"
    mc:Ignorable="d">
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ui:TitleBar
            x:Name="titleBar"
            VerticalContentAlignment="Center"
            IsExtendsContentIntoTitleBar="True"
            TitleHorizontalAlignment="Center">
            <ui:TitleBar.Title>
                <TextBlock Margin="10,0,0,0" Text="{x:Bind helper:LanguageService.GetString('Display_DragTheCodeToVerify')}" />
            </ui:TitleBar.Title>
        </ui:TitleBar>
        <WebView2
            x:Name="webView2"
            Grid.Row="1"
            DefaultBackgroundColor="Transparent">
            <WebView2.Resources>
                <SolidColorBrush x:Key="BrushForThemeBackgroundColor" Color="Transparent" />
            </WebView2.Resources>
        </WebView2>

    </Grid>
</base:WindowModelBase>
